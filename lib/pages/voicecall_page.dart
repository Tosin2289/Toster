import 'package:flutter/material.dart';

class VoiceCallPage extends StatelessWidget {
  const VoiceCallPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Voice Call PAGE"),
      ),
    );
  }
}
